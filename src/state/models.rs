pub mod accounts;
pub mod tasks;
pub mod workers;
/*use serde;
struct InferenceJob {
    link: String,
    id: u64,
}

struct InferenceBatch {
    jobs: Vec<InferenceJob>,
}

struct InferenceResult {
    result: Vec<Float>,
    id: u64,
    error: String,
}

struct ResultBatch {
    results: Vec<InferenceResult>,
}
*/
